{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "_site"
      }
    }
  ],
  "routes": [
    { "src": "/admin/config.yml", "dest": "/admin/config.yml" },
    { "src": "/admin", "dest": "/admin/index.html" }
  ]
}
